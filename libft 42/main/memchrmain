/*
int main() {
    const char *str = "Hello, World!";
    char search_char = 'W';
    size_t search_size = 8; // Taille de la recherche

    const void *result = memchr(str, search_char, search_size);

    if (result != NULL) {
        printf("Le caractère '%c' a été trouvé dans les premiers %zu octets.\n", search_char, search_size);
        size_t position = (size_t)(result - (void *)str);
        printf("Il se trouve à la position %zu dans la chaîne.\n", position);
    } else {
        printf("Le caractère '%c' n'a pas été trouvé dans les premiers %zu octets.\n", search_char, search_size);
    }

    return 0;
}
*/
